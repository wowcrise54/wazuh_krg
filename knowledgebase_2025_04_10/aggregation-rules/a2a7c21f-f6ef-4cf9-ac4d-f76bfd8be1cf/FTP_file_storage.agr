event FTP_storage:
    key:
        event_src.host, src.host, dst.host, id, importance, event_src.category, event_src.vendor, event_src.title
    action = "upload"
    object = "file"
    status = "success"
    protocol = "FTP"

aggregate FTP_file_storage: FTP_storage[3, 1000] within 1m