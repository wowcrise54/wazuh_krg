EVENTLOG = 'EventID.text="40028"'

COND = $Channel == "Application" and $Provider["Name"] == "MSExchangeIS"

action = "stop"
object = "database"
status = "success"

object.property = 'GUID of database'
object.value = $Data[1]
object.name = $Data[0]
object.process.id = $Data[2]

time = $TimeCreated["SystemTime"]
msgid = $EventID["text"]
importance = "low"

category.generic = "Database"
category.high = "Availability Management"
category.low = "Control"

event_src.vendor = "microsoft"
event_src.title = "exchange"
event_src.subsys = lower($Channel)
$first_dot = find_substr($Computer, '.')
if $first_dot != null then
    event_src.fqdn = lower($Computer)
    event_src.hostname = lower(substr($Computer, 0, $first_dot))
else
    event_src.hostname = lower($Computer)
endif
event_src.category = "Mail server"

id = "PT_Microsoft_Exchange_2016_eventlog_40028_a_MSExchangeIS_database_is_stopped"