!JSON = 'event.originator.module.name="Bulletins"'
!COND =  $event["actionType"]=="Bulletins:bulletinCreated"

subject = "account"
action = "create"
object = "resource"
status = "success"

subject.name = $userLogin
object.type = "bulletin"
object.id = $event["originator"]["id"]["id"]
src.ip = $userIp

time = $datetime
importance = "info"

$dot_pos = find_substr($systemIp, '.')
if maybe_ipv4($systemIp) != null then
   event_src.ip = $systemIp
elif $dot_pos != null then
   event_src.fqdn = $systemIp
   event_src.hostname = substr($systemIp, 0, $dot_pos)
else
   event_src.hostname = $systemIp
endif

event_src.title = "fcsp"
event_src.vendor = "positive_technologies"
event_src.category = "Other"

id = "PT_Positive_Technologies_Operators_Participants_Portal_filemonitor_Bulletins_bulletin_created"